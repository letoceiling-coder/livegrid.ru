# Технический запрос к поставщику JSON-фида

Уважаемые коллеги,

Для корректной интеграции вашего фида недвижимости в нашу платформу
просим предоставить ответы на следующие технические вопросы.

---

## 1. Основной endpoint

Пожалуйста, укажите базовый URL:

```
Endpoint: https://________________
```

---

## 2. Структура API — отдельные endpoints

Есть ли отдельные URL для следующих сущностей?
(отметьте нужные и укажите URL)

| Сущность        | Есть (да/нет) | URL |
|-----------------|---------------|-----|
| Жилые комплексы (ЖК / blocks / projects) | | |
| Корпуса / секции (buildings) | | |
| Квартиры (apartments / flats) | | |
| Застройщики (builders / developers) | | |
| Регионы / города (regions) | | |
| Метро / станции (subways) | | |
| Типы отделки (finishings) | | |
| Типы объектов (buildingtypes) | | |

---

## 3. Авторизация

Какой метод авторизации используется?

- [ ] Bearer token → `Authorization: Bearer <token>`
- [ ] Basic auth → `Authorization: Basic <base64(login:pass)>`
- [ ] Query parameter → `?token=<value>` или `?api_key=<value>`
- [ ] Только по IP-адресу (whitelist)
- [ ] Без авторизации

Если используется токен — пожалуйста, предоставьте:

```
Токен: ________________
Параметр (если query): ________________
Логин (если basic): ________________
Пароль (если basic): ________________
```

---

## 4. Геофильтрация

Поддерживает ли API фильтрацию по региону/городу?

- [ ] Да, параметр: `?region=________________`
- [ ] Да, параметр: `?city=________________`
- [ ] Да, параметр: `?city_id=________________`
- [ ] Нет, фид всегда возвращает все данные
- [ ] Данные уже отфильтрованы по Москве

---

## 5. Пагинация

Как реализована пагинация (если есть)?

- [ ] По номеру страницы: `?page=1&per_page=100`
- [ ] По offset: `?offset=0&limit=100`
- [ ] Нет пагинации (один большой файл)
- [ ] Курсор / next_url в ответе

Если есть пагинация:

```
Параметр страницы: ________________  (page / p / offset)
Параметр размера: ________________  (per_page / limit / count)
Максимальный размер страницы: ________________
Общее количество объектов: ________________
```

---

## 6. Объём данных

```
Приблизительное кол-во ЖК:          ________________
Приблизительное кол-во корпусов:     ________________
Приблизительное кол-во квартир:      ________________
Размер полного JSON (МБ):            ________________
```

---

## 7. Частота обновления

```
Как часто обновляются данные: ________________
  (в реальном времени / ежечасно / ежедневно / еженедельно)

Есть ли endpoint для получения только изменений (delta/diff): да / нет
Если да, URL: ________________
```

---

## 8. Временны́е метки

Присутствуют ли в объектах следующие поля?

| Поле | Есть | Название поля в вашем API |
|------|------|--------------------------|
| Дата создания | да/нет | |
| Дата обновления (updated_at) | да/нет | |
| Дата сдачи объекта | да/нет | |

---

## 9. Статусы / soft-delete

Как помечаются снятые с продажи или неактивные объекты?

- [ ] Поле `status` / `active` (укажите значения: ________________)
- [ ] Объект исчезает из фида
- [ ] Поле `deleted_at` (soft delete)
- [ ] Другое: ________________

---

## 10. Дополнительно

```
Есть ли тестовый endpoint или sandbox:    да / нет
Есть ли документация (Swagger / Postman): да / нет → URL: ________________
Есть ли rate limiting:                    да / нет → лимит: ________________
Есть ли версионирование API:              да / нет → текущая версия: ________________
```

---

## Контакт для технических вопросов

```
Имя: ________________
Email: ________________
Telegram / телефон: ________________
```

---

С уважением,
Команда Livegrid
tech@livegrid.ru
