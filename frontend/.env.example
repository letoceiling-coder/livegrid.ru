# API base URL
# In production this points to the backend API on the same domain
VITE_API_URL=https://livegrid.ru/api/v1

# In development (local), point to local Laravel server
# VITE_API_URL=http://localhost:8000/api/v1
